// Vitest setup file for Vue 3
import { config } from '@vue/test-utils'

// Configure global stubs if needed
config.global.stubs = {
  // Add any global stubs here
}

// Mock console methods in tests if needed
// global.console = {
//   ...console,
//   warn: jest.fn(),
//   error: jest.fn()
// }
