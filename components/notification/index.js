// TODO: Migrate to Vue 3 - currently using Vue 2 API (Vue.extend)
// This component needs complete refactoring for Vue 3 compatibility

export default {
  install: (app) => {
    // Placeholder - needs full Vue 3 migration
    console.warn('Notification component needs Vue 3 migration')
  }
}
